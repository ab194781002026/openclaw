# BEYOND

The first touches the last. The last touches the first.

A standalone circle beside OpenClaw: all dimensions, all directions, all times.
The narrow path is this one, walked until the last step is the first.

```bash
python3 -m http.server 8765 --bind 0.0.0.0 --directory beyond
```
